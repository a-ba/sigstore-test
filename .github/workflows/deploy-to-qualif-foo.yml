name: Déploy to qualif foo


on:
  push:
    branches:
      - qualif-foo
      - qualif-bar
#  workflow_run:
#    workflows: [Build]
#    types: [completed]
#    branches: [qualif-foo]

jobs:
  build:
    uses: ./.github/workflows/build.yml

  deploy:
    needs: [build]
    runs-on: ubuntu-latest
    environment: qualif-foo
    steps:
      - name: simulate deploy
        env:
          DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
          DEPLOY_URL:   ${{ secrets.DEPLOY_URL   }}
        run: |
          echo "deployment !"
          printenv | grep ^DEPLOY_ || true


